import React from 'react';

const FlagsList = (props) => (
  <div className = "profile__flags">
    {(props.flags) && //this will be props.flags[2], so that there must be at least three for it to show
      //unfinished - continue here
    }

  </div>
);

export { FlagsList as default };
